# Never commit real secrets — templates/ has *.example files only.
*.env
*.env.local
main.env
!*.env.op
!*.example
.DS_Store

# Local planning/scratch — never travels with the public repo (sanitization: no names/projects)
.hermes/
